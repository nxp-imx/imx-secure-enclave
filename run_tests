#!/bin/bash

tests=`find . -name '*shx'`

for test in ${tests}
do
    echo "---------------------------------------------------------------"
    echo ${test}
    echo "---------------------------------------------------------------"
    ./she_test ${test}
done
